import React from 'react';

interface AbdProps {
  // define props here
}

const Abd: React.FC<AbdProps> = (props) => {
  return (
    <div>
      Abd Component
    </div>
  );
};

export default Abd;

// ek api banani he jo agar folder delete karte he to mongodb se uska data hata de 
// agar folder delete hota he to uska data hatana he localstorage se 
